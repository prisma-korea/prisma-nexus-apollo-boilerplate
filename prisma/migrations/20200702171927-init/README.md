# Migration `20200702171927-init`

This migration has been generated by hyochan at 7/2/2020, 5:19:27 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "sample"."User" (
"birthDay" timestamp(3)   ,"createdAt" timestamp(3)   DEFAULT CURRENT_TIMESTAMP,"deletedAt" timestamp(3)   ,"email" text  NOT NULL ,"gender" "Gender"  ,"id" text  NOT NULL ,"name" text   ,"nickname" text   ,"password" text   ,"phone" text   ,"photoURL" text   ,"thumbURL" text   ,"updatedAt" timestamp(3)   DEFAULT CURRENT_TIMESTAMP,"verified" boolean   ,
    PRIMARY KEY ("id"))

CREATE TABLE "sample"."Profile" (
"authType" "AuthType"  ,"id" text  NOT NULL ,"socialId" text   ,"userId" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "sample"."Post" (
"content" text   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"deletedAt" timestamp(3)   ,"id" SERIAL,"published" boolean  NOT NULL DEFAULT false,"title" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"userId" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "sample"."User"("email")

CREATE UNIQUE INDEX "Profile.userId" ON "sample"."Profile"("userId")

ALTER TABLE "sample"."Profile" ADD FOREIGN KEY ("userId")REFERENCES "sample"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "sample"."Post" ADD FOREIGN KEY ("userId")REFERENCES "sample"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

DROP TYPE "AuthType_new"

DROP TYPE "Gender_new"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200702171927-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,59 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+enum Gender {
+  male
+  female
+}
+
+enum AuthType {
+  email
+  facebook
+  google
+  apple
+}
+
+model User {
+  id          String      @default(cuid()) @id
+  email       String      @unique
+  password    String?
+  name        String?
+  nickname    String?
+  thumbURL    String?
+  photoURL    String?
+  birthDay    DateTime?
+  gender      Gender?
+  phone       String?
+  verified    Boolean?
+  createdAt   DateTime?   @default(now())
+  updatedAt   DateTime?   @default(now())
+  deletedAt   DateTime?
+  posts       Post[]
+  profile     Profile?
+}
+
+model Profile {
+  id          String      @default(cuid()) @id
+  socialId    String?
+  authType    AuthType?
+  userId      String      @unique
+  User        User        @relation(fields: [userId], references: [id])
+}
+
+model Post {
+  id          Int         @default(autoincrement()) @id
+  title       String
+  content     String?
+  published   Boolean     @default(false)
+  userId      String
+  user        User        @relation(fields: [userId], references: [id])
+  createdAt   DateTime    @default(now())
+  updatedAt   DateTime    @default(now())
+  deletedAt   DateTime?
+}
```


